# Verified working on Python 3.13, 2026-09.
# The original work was done on Python 3.7; the models are plain NumPy/SciPy
# and still run unchanged, but these are the versions the example was tested on.
numpy==2.4.3
pandas==3.0.2
scipy==1.17.1
statsmodels==0.15.0
matplotlib==3.10.9
